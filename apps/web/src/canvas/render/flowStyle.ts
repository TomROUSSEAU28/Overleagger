/** Look of the animated currents. */
import type { FlowSymbol } from '@overleagger/core';

/** Default size of each symbol (px), used when the symbol is chosen. */
export const FLOW_SYMBOL_SIZE: Record<FlowSymbol, number> = {
  dot: 6,
  electron: 11,
  plus: 11,
  arrow: 9,
  dash: 10,
  comet: 7,
};

export const FLOW_COLOR = '@orange';
